<%= form_for(@line_item) do |f| %>

  <div class="form-inputs">
    <%= f.association :store_product %>
    <%= f.association :cart %>
  </div>

  <div class="form-actions">
    <%= f.button :submit %>
  </div>
<% end %>
